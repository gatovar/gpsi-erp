<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="bpm_candidate_rule" model="ir.rule">
            <field name="name">Project/Task: Employees need to be candidates</field>
            <field name="model_id" ref="project.model_project_task"/>
            <field name="domain_force">[('stage_id.gs_canditate_ids','!=',False), ('stage_id.gs_canditate_ids','in',[user.id])]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        </record>
        
    </data>
</odoo>
